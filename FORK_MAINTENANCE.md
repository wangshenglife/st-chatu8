# Fork 维护说明（请勿随意调整仓库结构）

本仓库是 [`damoshen123/st-chatu8`](https://github.com/damoshen123/st-chatu8) 的部署型 fork，用于在跟随作者更新的同时保留本仓库的定制功能。

## 仓库结构

- `main`
  - 唯一的稳定发布分支。
  - 云酒馆安装始终跟踪此分支。
  - 只能通过普通提交向前更新，禁止 force push、rebase、reset 或改写历史。
- `feature/manual-image-trigger-selections`
  - 「固定触发条目」功能的开发/参考分支。
  - 不要让云酒馆切换到此分支。
- `.github/workflows/sync-upstream.yml`
  - 定时取得上游最新 `main`，重新施加本仓库的定制修改，再提交到本仓库 `main`。
- `.github/upstream-main.sha`
  - 记录最近一次已处理的上游提交。
  - 请勿手工修改或删除。
- `FORK_MAINTENANCE.md`
  - 本维护说明；自动同步时必须保留。

## 自动更新方式

自动同步工作流每小时检查一次上游：

1. 读取作者仓库最新的 `main` 文件。
2. 以作者最新版为基础更新 fork。
3. 重新施加「固定触发条目」修改。
4. 通过 JavaScript 语法检查后，普通提交到 fork 的 `main`。

不要使用 GitHub 页面上的 **Sync fork** 按钮。GitHub 可能因为本仓库包含定制提交而显示“冲突”，这不代表插件存在 Bug，也不影响本仓库自己的自动同步流程。

## 禁止操作

- 禁止强推或重写 `main` 历史。
- 禁止把 `main` 重置成上游提交。
- 禁止更改默认分支或让云酒馆切换分支。
- 禁止删除、重命名或绕过自动同步工作流及其 SHA 记录文件。
- 禁止直接覆盖 `index.js` 后不更新定制修改的重新施加步骤。
- 禁止修改插件目录名、`display_name` 或其他会导致用户重新安装/重新导入的身份信息。
- 禁止向仓库提交聊天记录、用户设置、API Key、测试 Prompt、附件或 Railway 持久卷数据。

## 正确维护方式

- 上游作者更新：等待自动同步，不手工操作 fork 同步按钮。
- 修改定制功能：使用普通提交，并同步更新自动重新施加逻辑。
- 云酒馆更新：始终在 `main` 点击更新，不切换分支。
- `main-<提交号>` 中提交号发生变化是正常更新，不代表插件 ID 或配置发生变化。

如需改变以上结构，必须先确认不会破坏：

1. 上游自动同步；
2. 定制功能重新施加；
3. 云酒馆从现有 `main` 正常快进更新；
4. 用户现有配置和聊天数据继续保留。
